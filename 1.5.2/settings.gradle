pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url snapshotRepoUrl }
        maven { url releaseRepoUrl }
        mavenLocal()
    }
}

rootProject.name = "javax.mail"
